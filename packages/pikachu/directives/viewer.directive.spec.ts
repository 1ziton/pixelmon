import { ViewerDirective } from './viewer.directive';

describe('ViewerDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewerDirective();
    expect(directive).toBeTruthy();
  });
});
